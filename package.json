{
  "name": "adriancarolli",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-particles-js": "^2.0.0",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "yarn run build && yarn run surge",
    "surge": "surge ./build/ --domain adriancarolli.surge.sh"
  },
  "devDependencies": {
    "surge": "^0.19.0"
  }
}
